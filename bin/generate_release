#!/usr/bin/env node
/*
 * service-checker : bin-bootstrap/generate_release
 * Author: MrKMG (https://github.com/mrkmg)
 *
 * MIT License
 */

var path = require('path');
var fs   = require('fs');
var main  = path.join(path.dirname(fs.realpathSync(__filename)), '../main');

require(main + '/bin/generate_release')(process.argv);
