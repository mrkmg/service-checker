// Generated by CoffeeScript 1.10.0

/*
server-checker : lib/check-result
Author: MrKMG (https://github.com/mrkmg)
MIT License
 */

(function() {
  var CheckResult;

  CheckResult = (function() {
    function CheckResult(type) {
      this.type = type;
      this.start_time = Date.now();
      this._hr_start_time = process.hrtime();
    }

    CheckResult.prototype.time = -1;

    CheckResult.prototype.end_time = null;

    CheckResult.prototype.success = false;

    CheckResult.prototype.finished = function(error) {
      this._hr_end_time = process.hrtime(this._hr_start_time);
      this.time = Math.round((this._hr_end_time[0] * 1000) + (this._hr_end_time[1] / 1000000));
      this.end_time = Date.now();
      if (error) {
        this.error = error;
      } else {
        this.success = true;
      }
      return this;
    };

    return CheckResult;

  })();

  module.exports = CheckResult;

}).call(this);
